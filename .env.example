# Optional: override monitored URLs (comma-separated)
# TARGET_URLS=https://toyutoyu.com/app/,https://toyutoyu.com/

# Optional: cron schedule (hourly by default)
# CRON_SCHEDULE=0 * * * *
# CRON_TIMEZONE=Asia/Tokyo

# Optional: request timeout
# TIMEOUT_MS=10000

# Optional: LINE notification (Push)
# LINE_CHANNEL_ACCESS_TOKEN=
# LINE_TO=
# LINE_BROADCAST=1

# Optional: LINE webhook signature verification (only if you use /callback)
# LINE_CHANNEL_SECRET=
